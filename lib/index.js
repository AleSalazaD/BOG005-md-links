const { isMdFile } = require('../mdFiles');
const { read } = require('../mdSearchLinks');
const { pathAbsoluteExists } = require('../mdPathAbsolute');
const terminal = process.argv[2]

// funcion para validar los links
mdLinks = (path, options = {validate: false}) => {
    return new Promise((resolve, reject) => {
        
    })
}

mdLinks(terminal)
module.exports = {
     
};